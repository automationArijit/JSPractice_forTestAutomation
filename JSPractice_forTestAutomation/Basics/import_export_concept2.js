
import getdata, {pi,name,sumOfTwoNumber,arw,anyo} from './import_export-concept1.js'

//getdata ----> is a default function. we can rename it: as arijit, but must have to call then arijit()
// { } --->> is for non default property.
sumOfTwoNumber(20,89); //The sum  of 20 and 89 is: 109
console.log(pi); //3.14
console.log(name); //Raksesh

anyo();
arw();
getdata();
